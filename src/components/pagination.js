import React, { useState } from 'react'

function Pagination() {
    return (
        <div>
            <button>previous</button>
            <button>next</button>
        </div>
    )
}

export default Pagination;